apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: exam
  labels:
    k8s-app: exam-app
data:
  env.js: |-
    var env = {
        apiUrl: "http://{{ .Values.api_hostname }}"
    };

